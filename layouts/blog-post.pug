extends /_layout

block content
  article.prose(class="xl:prose-lg")
    h1(class="!mb-6")!= md.renderInline(title)
    .mb-6
      +postMeta(page.date, updated, tags)
    p.lead!= md.renderInline(intro)
    hr(aria-hidden="true" class="!mb-0")

    //- Anchor for home page's "Continue reading" links
    #continue-reading.mb-12

    +toc(content)
    != content
