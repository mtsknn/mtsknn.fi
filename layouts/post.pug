extends ./layout

block content
  article.prose
    h1(class="!mb-6")= title
    .mb-6
      +post_meta(page.date, tags)
    .prose-xl!= md.render(intro)

    //- Anchor for home page's "Continue reading" links
    #continue-reading.mb-12

    != content
