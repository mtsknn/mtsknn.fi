//- The order doesn't matter (see https://stackoverflow.com/q/1274974/1079869),
//- but the outcome looks better when the lines are sorted
each page in [...collections.all].sort(sort.byUrl)
  = `${site.url}${page.url}\n`
