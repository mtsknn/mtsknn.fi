const entity = require('../../../data/entity')
const site = require('../../../data/site')

module.exports = {
  title: `${site.title} ${entity.ndash} Cookbook`,
  description: 'My technical recipes.',
  collectionsKey: 'cookbookRecipes',
}
